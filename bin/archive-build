#!/usr/bin/env bash
set -eu

export PATH=~/.gem/ruby/2.3.0/bin:$PATH
export BUNDLE_GEMFILE=../webapp/Gemfile

gem install bundler -N -v 1.17 --user-install
bundle install

bundle exec ../dev/releasenotes-md-to-yaml.rb

bundle exec bin/archive-build.rb
